<html lang="en">
  <%- include('./partials/head');%>

  <link rel="icon" href="/img/logo-for-mh.png" type="image/png">

  <style>

/* Основные стили для тела страницы */
body {
  font-family: 'Arial', sans-serif;
  color: #333;
  justify-content: center;
  align-items: center;
  height: 100vh;
  margin: 0;
  overflow: hidden; /* Это поможет избежать полос прокрутки, если фон выходит за границы */
  background: linear-gradient(135deg, #324B81, #47633F);
  background-size: 400% 400%;
  animation: gradientMove 15s ease infinite;
}

/* Анимация для плавного движения фона */
@keyframes gradientMove {
  0% {
    background-position: 0% 0%;
  }
  50% {
    background-position: 100% 100%;
  }
  100% {
    background-position: 0% 0%;
  }
}

/* Стиль для заголовка изображения */
.collections-logo {
  display: block;
  margin: 0 auto 20px;
  max-width: 350px; /* Увеличиваем размер иконки */
  height: auto;
}

/* Контейнер для основной формы */
.container {
  margin-top: 10px;
  background: #ffffff;
  border-radius: 15px;
  box-shadow: 0 8px 16px rgba(0, 0, 0, 0.2);
  padding: 40px;
  max-width: 600px; /* Увеличиваем ширину контейнера */
  width: 100%;
  border: 1px solid #ddd;
  text-align: center; /* Центрируем текст */
}

/* Стиль для заголовка */
h1 {
  color: #324B81;
  font-size: 2rem;
  margin-bottom: 30px;
}

/* Стили для списка коллекций */
ul {
  list-style-type: none; /* Убираем маркеры */
  padding: 0;
  margin: 0;
}

li {
  margin-bottom: 20px; /* Увеличиваем отступы между элементами списка */
}

button {
  display: block;
  width: 100%;
  border: none;
  border-radius: 10px; /* Округляем углы кнопки */
  padding: 15px;
  background: linear-gradient(135deg, #324B81, #47633F);
  color: white;
  font-size: 18px;
  font-weight: 600;
  cursor: pointer;
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
  transition: background 0.3s ease, transform 0.2s ease;
}

/* Эффект при наведении на кнопку */
button:hover {
  background: linear-gradient(135deg, #47633F, #324B81);
  transform: scale(1.02);
}

a.collection {
  display: block;
  color: #ffffff;
  text-decoration: none;
}

a.collection:hover {
  text-decoration: none;
}

/* Стили для кнопки выхода */
#to_collections {
  color: #333;
  background: #ffffff;
  border: none;
  border-radius: 10px;
  padding: 15px 30px;
  text-align: center;
  display: inline-block;
  margin: 30px 30px;
  font-size: 16px;
  font-weight: 600;
  text-decoration: none;
  box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
  transition: background 0.3s ease, transform 0.2s ease;
}

#to_collections:hover {
  background: #47633F;
  transform: scale(1.02);
  color: #ffffff;
}

/* Стиль для футера */
.footer {
  background-color: #ffffff;
  width: 100%;
  height: 80px;
  position: absolute;
  bottom: 0;
  color: #333;
  border-top: 2px solid #ddd;
  text-align: center;
  padding-top: 10px;
}

.footer h4, .footer p {
  margin: 5px 0;
}



  </style>

    <body>
      <a id="to_collections" href="/api/login">Вийти з аккаунту</a>
      <img id="headerImage" src="/img/logo-for-military-hospital (1).png" alt="Military Hospital Logo" class="collections-logo">
      <div class="container">
        <h1>Всі донати</h1>
        <ul>
          <% for (const collection of collections) { %>
            <li><button id="collectionButton"><a class="collection"href="/api/collections/<%= collection.name %>"><%= collection.name.toUpperCase() %></a></button></li>
          <% } %>
        </ul>
      </div>
      <div class="footer">
        <h4>🎔Всі права захищені🎔</h4>
        <p>by vamais <br> ฅ•ω•ฅ</p>
      </div>
    </body>
</html>
